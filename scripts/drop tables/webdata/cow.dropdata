// Flattened cow.rs2's [ai_queue3,_cow] table. No random roll at all in the
// source - both drops are unconditional obj_add calls, i.e. guaranteed
// every kill.
[cow]
table=drop_table
data=total,0
data=guaranteed,raw_beef,1
data=guaranteed,cow_hide,1
